torch>=2.4
transformers>=4.46
datasets>=2.20
accelerate>=0.34
huggingface-hub>=0.24
safetensors>=0.4
sentencepiece>=0.2
protobuf>=4.25
pyyaml>=6.0
tqdm>=4.66
numpy>=1.26
pandas>=2.0
pyarrow>=15.0
wandb>=0.17
# ---- Remote execution on Modal (CPU-only client; the H100 image is built
# from the spec in modal_app.py). ------------------------------------------
modal>=0.66
python-dotenv>=1.0
# Optional: enables Flash-Attention-2. The code falls back to PyTorch SDPA
# (which itself uses FA2 kernels when available) if flash-attn is missing.
# flash-attn>=2.5
